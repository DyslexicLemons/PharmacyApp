fastapi==0.115.2
uvicorn==0.30.6
SQLAlchemy==2.0.35
alembic>=1.13.0
pydantic==2.9.2
pydantic-core==2.23.4
python-multipart==0.0.9
bcrypt>=4.0.0
PyJWT>=2.8.0
slowapi>=0.1.9
python-dotenv>=1.0.0
redis>=5.0.0

# ── AWS (optional — only needed in production / ECS) ──────────────────────
boto3>=1.34.0          # Secrets Manager; omit if not deploying to AWS

# ── Test dependencies ─────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # required by FastAPI TestClient
